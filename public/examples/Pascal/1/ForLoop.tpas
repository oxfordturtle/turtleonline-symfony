PROGRAM ForLoop;
VAR count: integer;
BEGIN
  for count := 1 to 200 do
    begin
      {move forward and right}
      forward(count div 3);
      right(5);
      {draw a red circle with a black outline}
      colour(red);
      blot(200);
      colour(black);
      circle(200)
    end
END.
