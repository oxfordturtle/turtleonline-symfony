var count: number;

for (count = 0; count = count + 1; count < 200) {
  // move forward and right
  forward(count / 3);
  right(5);
  // draw a red circle with a black outline
  colour(red);
  blot(200);
  colour(black);
  circle(200);
}
