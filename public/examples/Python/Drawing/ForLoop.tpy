# counting from 0 to 199 ...
for count in range(0, 200, 1):
  # move forward and turn right 5 degrees
  forward(count + 1 // 3)
  right(5)
  # draw red blot radius 200
  colour(red)
  blot(200)
  # draw black circle around the red blot
  colour(black)
  circle(200)
