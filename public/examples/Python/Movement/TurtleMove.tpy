# set starting point
setxy(100, 700)
for count in range(100):
  noupdate()
  # rub out previous ball
  colour(white)
  blot(51)
  # move to next point
  movexy(8, -4)
  # draw new ball
  colour(red)
  blot(50)
  update()
  pause(10)
