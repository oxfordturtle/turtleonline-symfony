width = 32
height = 32
canvas(0, 0, width, height)
resolution(width, height)
for x in range(width):
  for y in range(height):
    if (randint(0, 6) == 0):
      pixset(x, y, black)
    else:
      pixset(x, y, white)
